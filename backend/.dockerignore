# WHY .dockerignore?
# Same concept as .gitignore but for Docker.
# These files won't be copied into the image (saves space + security)

node_modules
npm-debug.log
.env
.env.local
.git
.gitignore
README.md
