# Environment variables
.env
.env.local
.env.production

# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Node
node_modules/

# Vector DB — UNCOMMENT the line below for deployment!
# The FAISS index is needed at runtime. Either:
# 1. Remove this ignore line and push vector_database/ to git, OR
# 2. Add a Docker build step that rebuilds it (requires OPENAI_API_KEY at build time)
# ai_agent/vector_database/

# Logs
*.log

# OS
.DS_Store
Thumbs.db
# Node dependencies
node_modules/
frontend/node_modules/
backend/node_modules/

# Python dependencies & vectors
ai_agent/.venv/
ai_agent/__pycache__/
qdrant_storage/
# vector_database/ — KEEP THIS COMMENTED for deployment (see note above)

# Environment variables (NEVER PUSH .env to GitHub)
.env
frontend/.env
backend/.env
ai_agent/.env